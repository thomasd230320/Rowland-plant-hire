// Keystatic ships its own full-page UI and styling, so this layout stays bare.
export default function KeystaticLayout({ children }) {
  return children
}

export const metadata = {
  title: 'Edit website',
  robots: { index: false, follow: false },
}
